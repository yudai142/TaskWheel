<%= @resource.email %> 様

パスワード再設定の依頼を受け付けました。
以下のリンクから新しいパスワードを設定してください。

<%= "#{ENV.fetch('APP_BASE_URL', 'http://localhost:3000')}/password-reset?token=#{CGI.escape(@token)}" %>

このメールに心当たりがない場合は、このメールを破棄してください。
